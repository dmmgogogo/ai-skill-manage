# AI Skill Manager — M1 实施计划（基线：扫描与只读展示）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从零搭出 SwiftUI macOS App 骨架，能扫描并以三栏布局只读展示 4 个用户级 skill 源（Claude / Codex / Cursor Rules / Cursor Skills）。完成后用户能浏览和搜索本机所有 skill，但不能编辑。

**Architecture:** 4 层架构（View / ViewModel / Repository / Infrastructure），协议化的 `SkillRepository` 让 4 个数据源各自实现，`@Observable` 的 `AppStore` 作为单一可观察源。Raw 编辑器与编辑流程在 M2 中加入，本计划只做"加载到屏幕"。

**Tech Stack:** Swift 5.9+ / SwiftUI / @Observable / macOS 14 Sonoma / Yams（YAML 解析）/ XCTest

**Spec:** `docs/superpowers/specs/2026-04-28-ai-skill-manager-design.md`

---

## 文件结构

```
ai-skill-manage/
├── AISkillManager.xcodeproj/                          (Xcode 项目)
├── AISkillManager/                                     (App target)
│   ├── AISkillManagerApp.swift                         // @main
│   ├── App/
│   │   └── AppRoot.swift                               // 三栏 NavigationSplitView
│   ├── Views/
│   │   ├── SidebarView.swift                           // 4 个数据源
│   │   ├── ItemListView.swift                          // 当前源的条目列表
│   │   └── DetailView.swift                            // M1 只读展示 rawContent
│   ├── ViewModels/
│   │   └── AppStore.swift                              // @Observable，唯一状态源
│   ├── Models/
│   │   ├── SourceKind.swift
│   │   ├── SourceScope.swift
│   │   ├── Project.swift
│   │   ├── SkillItemID.swift
│   │   └── SkillItem.swift
│   ├── Repositories/
│   │   ├── SkillRepository.swift                       // 协议 + 工厂
│   │   ├── DirectorySkillRepoBase.swift                // 目录式公共逻辑
│   │   ├── ClaudeSkillsRepo.swift
│   │   ├── CodexSkillsRepo.swift
│   │   ├── CursorSkillsRepo.swift
│   │   └── CursorRulesRepo.swift
│   ├── Infrastructure/
│   │   └── YAMLFrontmatter.swift                       // 浅解析 name/description
│   └── Resources/
│       └── Assets.xcassets
└── AISkillManagerTests/                                (Tests target)
    ├── Fixtures/                                        // 集成测试用样本
    │   ├── claude-good/SKILL.md
    │   ├── claude-bad-yaml/SKILL.md
    │   ├── claude-empty-desc/SKILL.md
    │   └── cursor-rule.mdc
    ├── YAMLFrontmatterTests.swift
    ├── DirectorySkillRepoBaseTests.swift
    ├── CursorRulesRepoTests.swift
    └── AppStoreTests.swift
```

---

## Task 1: Bootstrap Xcode 项目

**Files:**
- Create: `AISkillManager.xcodeproj/` (Xcode 生成)
- Create: `AISkillManager/AISkillManagerApp.swift`
- Create: `.gitignore` 追加项

**说明：** 这一步必须手动用 Xcode 创建项目，CLI 不能完整生成 Mac App bundle 的所有元数据。

- [ ] **Step 1: 在 Xcode 创建项目**

打开 Xcode 15 或更高版本，菜单 File → New → Project，按以下配置：

- Multiplatform / **App**（注意是 App，不是 Document App）
- Product Name: `AISkillManager`
- Team: 选你的 Apple ID（无 ID 也可以，本地能跑）
- Organization Identifier: `com.yourname`（任意）
- Bundle Identifier: 自动 = `com.yourname.AISkillManager`
- Interface: **SwiftUI**
- Language: **Swift**
- 取消勾选 "Include Tests" — 我们后面单独加 Test target（避免默认模板的多余文件）
- Save 到 `/Users/mmx/Documents/work/Github/ai-skill-manage/`，**注意必须是仓库根目录**

创建完成后：
- 删除自动生成的 `ContentView.swift`（我们后面会创建 `AppRoot.swift`）
- 修改 `AISkillManagerApp.swift`：

```swift
import SwiftUI

@main
struct AISkillManagerApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Hello, AI Skill Manager")
        }
    }
}
```

- 在 Project Navigator 选中项目根 → 选 AISkillManager target → General：
  - **Minimum Deployments → macOS 14.0**
- Build Settings 搜 "Swift Language Version" → 设 Swift 5.9+

- [ ] **Step 2: 添加 Test Target**

Xcode 菜单 File → New → Target → macOS → **Unit Testing Bundle**：
- Product Name: `AISkillManagerTests`
- Target to be Tested: `AISkillManager`

- [ ] **Step 3: 添加 Yams 依赖**

Xcode 菜单 File → Add Package Dependencies → URL 输入 `https://github.com/jpsim/Yams.git` → 版本规则 "Up to Next Major" 5.0.0 → 把 Yams 加到 `AISkillManager` target。

- [ ] **Step 4: 在 AISkillManager target 创建源码目录**

在 Xcode Project Navigator 右键 AISkillManager → New Group："App"、"Views"、"ViewModels"、"Models"、"Repositories"、"Infrastructure"。Group 不必对应磁盘目录，但**必须勾选 "Create folders"** 这样磁盘上也是目录结构。

- [ ] **Step 5: 验证 Build**

⌘B 编译。预期：通过，左下角 Build Succeeded。
⌘R 运行。预期：弹出窗口，显示 "Hello, AI Skill Manager"。
关闭窗口。

- [ ] **Step 6: Commit**

```bash
git add AISkillManager.xcodeproj AISkillManager/
git commit -m "feat: bootstrap Xcode project with macOS 14 / SwiftUI / Yams"
```

---

## Task 2: 定义数据模型类型

**Files:**
- Create: `AISkillManager/Models/SourceKind.swift`
- Create: `AISkillManager/Models/SourceScope.swift`
- Create: `AISkillManager/Models/Project.swift`
- Create: `AISkillManager/Models/SkillItemID.swift`
- Create: `AISkillManager/Models/SkillItem.swift`

- [ ] **Step 1: 创建 SourceKind.swift**

```swift
import Foundation

enum SourceKind: String, Codable, CaseIterable, Hashable {
    case claudeSkills
    case codexSkills
    case cursorSkills
    case cursorRules

    var displayName: String {
        switch self {
        case .claudeSkills: return "Claude Skills"
        case .codexSkills:  return "Codex Skills"
        case .cursorSkills: return "Cursor Skills"
        case .cursorRules:  return "Cursor Rules"
        }
    }

    var iconSymbol: String {
        switch self {
        case .claudeSkills: return "brain"
        case .codexSkills:  return "bolt"
        case .cursorSkills: return "target"
        case .cursorRules:  return "ruler"
        }
    }
}
```

- [ ] **Step 2: 创建 Project.swift**

```swift
import Foundation

struct Project: Identifiable, Codable, Hashable {
    let id: UUID
    var name: String
    var path: URL
    var addedAt: Date

    init(id: UUID = UUID(), name: String, path: URL, addedAt: Date = Date()) {
        self.id = id
        self.name = name
        self.path = path
        self.addedAt = addedAt
    }
}
```

- [ ] **Step 3: 创建 SourceScope.swift**

```swift
import Foundation

enum SourceScope: Equatable, Hashable {
    case user
    case project(Project)

    var key: String {
        switch self {
        case .user: return "user"
        case .project(let p): return "project:\(p.id.uuidString)"
        }
    }

    var displayLabel: String {
        switch self {
        case .user: return "用户级"
        case .project(let p): return "项目: \(p.name)"
        }
    }
}
```

- [ ] **Step 4: 创建 SkillItemID.swift**

```swift
import Foundation
import CryptoKit

struct SkillItemID: Hashable, Codable {
    let kind: SourceKind
    let scopeKey: String
    let pathFingerprint: String

    static func make(kind: SourceKind, scope: SourceScope, mainFileURL: URL) -> SkillItemID {
        let path = mainFileURL.standardizedFileURL.path
        let digest = Insecure.SHA1.hash(data: Data(path.utf8))
        let hex = digest.map { String(format: "%02x", $0) }.joined()
        return SkillItemID(
            kind: kind,
            scopeKey: scope.key,
            pathFingerprint: String(hex.prefix(12))
        )
    }
}
```

- [ ] **Step 5: 创建 SkillItem.swift**

```swift
import Foundation

struct SkillItem: Identifiable, Hashable {
    let id: SkillItemID
    let kind: SourceKind
    let scope: SourceScope
    let mainFileURL: URL
    let containerURL: URL?
    var name: String
    var description: String
    var rawContent: String
    var fileModifiedAt: Date
    var sizeBytes: Int
    var hasSubdirectories: Bool
}
```

- [ ] **Step 6: 验证 Build**

⌘B。预期：Build Succeeded。

- [ ] **Step 7: Commit**

```bash
git add AISkillManager/Models/
git commit -m "feat(model): define core types (SourceKind/Scope/Project/SkillItemID/SkillItem)"
```

---

## Task 3: YAMLFrontmatter 浅解析

**Files:**
- Create: `AISkillManager/Infrastructure/YAMLFrontmatter.swift`
- Test: `AISkillManagerTests/YAMLFrontmatterTests.swift`

**职责：** 从一份 markdown 文件原文中提取 frontmatter 区块（`---` 之间的内容），用 Yams 解析出 `name` 和 `description` 字段。失败时返回空字段，不抛错——保证业务流程不被异常 frontmatter 阻断。

- [ ] **Step 1: 写失败的测试**

`AISkillManagerTests/YAMLFrontmatterTests.swift`:

```swift
import XCTest
@testable import AISkillManager

final class YAMLFrontmatterTests: XCTestCase {
    func test_parse_normal_frontmatter() {
        let content = """
        ---
        name: design
        description: Use when building UI
        ---

        # Design skill
        body content
        """

        let result = YAMLFrontmatter.parseShallow(from: content)
        XCTAssertEqual(result.name, "design")
        XCTAssertEqual(result.description, "Use when building UI")
    }

    func test_parse_no_frontmatter() {
        let content = "Just markdown without frontmatter"
        let result = YAMLFrontmatter.parseShallow(from: content)
        XCTAssertNil(result.name)
        XCTAssertNil(result.description)
    }

    func test_parse_invalid_yaml() {
        let content = """
        ---
        name: design
        description: : invalid : yaml :
        ---

        body
        """

        let result = YAMLFrontmatter.parseShallow(from: content)
        // 解析失败时不抛错，返回 nil
        XCTAssertNil(result.name)
        XCTAssertNil(result.description)
    }

    func test_parse_quoted_strings() {
        let content = """
        ---
        name: "playwright"
        description: "Use when automating a browser"
        ---
        """

        let result = YAMLFrontmatter.parseShallow(from: content)
        XCTAssertEqual(result.name, "playwright")
        XCTAssertEqual(result.description, "Use when automating a browser")
    }

    func test_parse_only_name() {
        let content = """
        ---
        name: minimal
        ---
        """

        let result = YAMLFrontmatter.parseShallow(from: content)
        XCTAssertEqual(result.name, "minimal")
        XCTAssertNil(result.description)
    }

    func test_parse_with_extra_fields() {
        // 多余字段不影响 name/description 提取
        let content = """
        ---
        name: design
        description: foo
        metadata:
          version: "3.8.0"
        alwaysApply: true
        ---
        """

        let result = YAMLFrontmatter.parseShallow(from: content)
        XCTAssertEqual(result.name, "design")
        XCTAssertEqual(result.description, "foo")
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Xcode 菜单 Product → Test（⌘U），或单测试导航点 ◇。
预期：6 个测试全失败（"YAMLFrontmatter not found"）。

- [ ] **Step 3: 实现 YAMLFrontmatter.swift**

```swift
import Foundation
import Yams

enum YAMLFrontmatter {
    struct Shallow {
        var name: String?
        var description: String?
    }

    /// 从 markdown 原文中浅解析 frontmatter，仅提取 name 和 description。
    /// 解析失败时返回空字段（不抛错）。
    static func parseShallow(from content: String) -> Shallow {
        guard let yamlBlock = extractFrontmatterBlock(content) else {
            return Shallow()
        }

        do {
            guard let dict = try Yams.load(yaml: yamlBlock) as? [String: Any] else {
                return Shallow()
            }
            return Shallow(
                name: dict["name"] as? String,
                description: dict["description"] as? String
            )
        } catch {
            return Shallow()
        }
    }

    /// 提取首个 `---` ... `---` 之间的内容。文件不以 `---` 起始时返回 nil。
    private static func extractFrontmatterBlock(_ content: String) -> String? {
        let lines = content.components(separatedBy: "\n")
        guard let first = lines.first, first.trimmingCharacters(in: .whitespaces) == "---" else {
            return nil
        }
        var block: [String] = []
        for line in lines.dropFirst() {
            if line.trimmingCharacters(in: .whitespaces) == "---" {
                return block.joined(separator: "\n")
            }
            block.append(line)
        }
        return nil // 没找到结尾的 `---`
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

⌘U。预期：6 个测试全 PASS。

- [ ] **Step 5: Commit**

```bash
git add AISkillManager/Infrastructure/YAMLFrontmatter.swift AISkillManagerTests/YAMLFrontmatterTests.swift
git commit -m "feat(infra): add YAMLFrontmatter shallow parser with tests"
```

---

## Task 4: SkillRepository 协议

**Files:**
- Create: `AISkillManager/Repositories/SkillRepository.swift`

- [ ] **Step 1: 实现协议**

```swift
import Foundation

protocol SkillRepository {
    /// 该仓库对应的源类型（用于回填 SkillItem.kind）
    var kind: SourceKind { get }

    /// 该仓库对应的范围（用户级 / 某项目）
    var scope: SourceScope { get }

    /// 扫描根目录是否存在
    var rootExists: Bool { get }

    /// 列出该源下所有 skill 项。目录或文件不存在时返回空数组（不抛错）。
    func listAll() throws -> [SkillItem]
}
```

**说明：** M1 只需 `listAll`。`save` / `delete` / `create` 在 M2 / M3 加入。

- [ ] **Step 2: 验证 Build**

⌘B。预期：Build Succeeded。

- [ ] **Step 3: Commit**

```bash
git add AISkillManager/Repositories/SkillRepository.swift
git commit -m "feat(repo): introduce SkillRepository protocol"
```

---

## Task 5: DirectorySkillRepoBase（目录式公共逻辑）

**Files:**
- Create: `AISkillManager/Repositories/DirectorySkillRepoBase.swift`
- Test: `AISkillManagerTests/DirectorySkillRepoBaseTests.swift`
- Test fixture: `AISkillManagerTests/Fixtures/`（在 Xcode 中加目录引用）

**职责：** 给 Claude / Codex / Cursor-Skills 三家共享 "扫一个根目录、每个子目录里找 SKILL.md" 的逻辑。

- [ ] **Step 1: 在 Xcode 中创建 Fixtures 目录**

Xcode 中右键 AISkillManagerTests → New Group → "Fixtures"（**勾选 Create folder reference**，不是 Group）。

在磁盘上 `AISkillManagerTests/Fixtures/` 下创建以下文件：

`AISkillManagerTests/Fixtures/dir-source/skill-good/SKILL.md`:
```markdown
---
name: skill-good
description: Good skill with both fields
---

# skill-good

Body content here.
```

`AISkillManagerTests/Fixtures/dir-source/skill-no-desc/SKILL.md`:
```markdown
---
name: skill-no-desc
---

# Without description
```

`AISkillManagerTests/Fixtures/dir-source/skill-bad-yaml/SKILL.md`:
```markdown
---
name: skill-bad-yaml
description: : broken : :
---

Body
```

`AISkillManagerTests/Fixtures/dir-source/skill-with-subs/SKILL.md`:
```markdown
---
name: skill-with-subs
description: Has sub directories
---

Body
```

`AISkillManagerTests/Fixtures/dir-source/skill-with-subs/references/note.md`:
```markdown
sub note
```

`AISkillManagerTests/Fixtures/dir-source/skill-with-subs/scripts/run.sh`:
```bash
#!/bin/bash
echo hi
```

把这些文件加到 Xcode 的 AISkillManagerTests target（右键 → Add Files To "AISkillManagerTests"）。

- [ ] **Step 2: 写失败的测试**

`AISkillManagerTests/DirectorySkillRepoBaseTests.swift`:

```swift
import XCTest
@testable import AISkillManager

final class DirectorySkillRepoBaseTests: XCTestCase {
    private var fixtureRoot: URL {
        Bundle(for: type(of: self))
            .url(forResource: "dir-source", withExtension: nil)!
    }

    private func makeRepo() -> DirectorySkillRepoBase {
        DirectorySkillRepoBase(
            kind: .claudeSkills,
            scope: .user,
            root: fixtureRoot
        )
    }

    func test_rootExists_true_when_directory_present() {
        XCTAssertTrue(makeRepo().rootExists)
    }

    func test_rootExists_false_when_directory_missing() {
        let bogus = fixtureRoot.appendingPathComponent("does-not-exist")
        let repo = DirectorySkillRepoBase(kind: .claudeSkills, scope: .user, root: bogus)
        XCTAssertFalse(repo.rootExists)
    }

    func test_listAll_returns_all_skills() throws {
        let items = try makeRepo().listAll()
        let names = Set(items.map(\.name))
        XCTAssertEqual(names, ["skill-good", "skill-no-desc", "skill-bad-yaml", "skill-with-subs"])
    }

    func test_listAll_extracts_name_and_description() throws {
        let items = try makeRepo().listAll()
        let good = items.first { $0.name == "skill-good" }!
        XCTAssertEqual(good.description, "Good skill with both fields")
    }

    func test_listAll_uses_directory_name_when_frontmatter_name_missing() throws {
        // skill-bad-yaml 的 frontmatter 解析失败，name 应回退为目录名
        let items = try makeRepo().listAll()
        let bad = items.first { $0.mainFileURL.path.contains("skill-bad-yaml") }!
        XCTAssertEqual(bad.name, "skill-bad-yaml")
        XCTAssertEqual(bad.description, "")
    }

    func test_listAll_detects_subdirectories() throws {
        let items = try makeRepo().listAll()
        let withSubs = items.first { $0.name == "skill-with-subs" }!
        XCTAssertTrue(withSubs.hasSubdirectories)

        let good = items.first { $0.name == "skill-good" }!
        XCTAssertFalse(good.hasSubdirectories)
    }

    func test_listAll_includes_raw_content() throws {
        let items = try makeRepo().listAll()
        let good = items.first { $0.name == "skill-good" }!
        XCTAssertTrue(good.rawContent.contains("Body content here."))
        XCTAssertTrue(good.rawContent.hasPrefix("---"))
    }

    func test_listAll_skips_files_at_root() throws {
        // 只扫子目录，不扫根目录下的散文件
        let extraFile = fixtureRoot.appendingPathComponent("loose-file.md")
        try? "loose".write(to: extraFile, atomically: true, encoding: .utf8)
        defer { try? FileManager.default.removeItem(at: extraFile) }

        let items = try makeRepo().listAll()
        XCTAssertFalse(items.contains { $0.name == "loose-file" })
    }

    func test_listAll_skips_dotfiles() throws {
        // 跳过 .DS_Store 之类
        let dsStore = fixtureRoot.appendingPathComponent(".DS_Store")
        try? "x".write(to: dsStore, atomically: true, encoding: .utf8)
        defer { try? FileManager.default.removeItem(at: dsStore) }

        let items = try makeRepo().listAll()
        XCTAssertFalse(items.contains { $0.name.hasPrefix(".") })
    }

    func test_listAll_returns_empty_for_missing_root() throws {
        let bogus = fixtureRoot.appendingPathComponent("does-not-exist")
        let repo = DirectorySkillRepoBase(kind: .claudeSkills, scope: .user, root: bogus)
        let items = try repo.listAll()
        XCTAssertTrue(items.isEmpty)
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

⌘U。预期：所有测试 fail（`DirectorySkillRepoBase` not found）。

- [ ] **Step 4: 实现 DirectorySkillRepoBase.swift**

```swift
import Foundation

class DirectorySkillRepoBase: SkillRepository {
    let kind: SourceKind
    let scope: SourceScope
    let root: URL

    init(kind: SourceKind, scope: SourceScope, root: URL) {
        self.kind = kind
        self.scope = scope
        self.root = root
    }

    var rootExists: Bool {
        var isDir: ObjCBool = false
        let exists = FileManager.default.fileExists(atPath: root.path, isDirectory: &isDir)
        return exists && isDir.boolValue
    }

    func listAll() throws -> [SkillItem] {
        guard rootExists else { return [] }

        let fm = FileManager.default
        let entries = (try? fm.contentsOfDirectory(at: root,
                                                   includingPropertiesForKeys: [.isDirectoryKey, .contentModificationDateKey, .fileSizeKey],
                                                   options: [.skipsHiddenFiles])) ?? []

        var items: [SkillItem] = []
        for dir in entries {
            // 只看子目录，跳过散文件
            var isDir: ObjCBool = false
            guard fm.fileExists(atPath: dir.path, isDirectory: &isDir), isDir.boolValue else {
                continue
            }
            // 跳过点目录（虽然 skipsHiddenFiles 已过滤，但双保险）
            if dir.lastPathComponent.hasPrefix(".") { continue }

            let mainFileURL = dir.appendingPathComponent("SKILL.md")
            guard fm.fileExists(atPath: mainFileURL.path) else { continue }

            guard let item = makeItem(containerURL: dir, mainFileURL: mainFileURL) else { continue }
            items.append(item)
        }
        return items.sorted { $0.name.localizedCaseInsensitiveCompare($1.name) == .orderedAscending }
    }

    private func makeItem(containerURL: URL, mainFileURL: URL) -> SkillItem? {
        guard let content = try? String(contentsOf: mainFileURL, encoding: .utf8) else { return nil }

        let attrs = (try? FileManager.default.attributesOfItem(atPath: mainFileURL.path)) ?? [:]
        let mtime = (attrs[.modificationDate] as? Date) ?? Date()
        let size = (attrs[.size] as? Int) ?? 0

        let parsed = YAMLFrontmatter.parseShallow(from: content)
        let dirName = containerURL.lastPathComponent
        let name = parsed.name ?? dirName
        let desc = parsed.description ?? ""

        let hasSubs = detectSubdirectories(in: containerURL)

        return SkillItem(
            id: SkillItemID.make(kind: kind, scope: scope, mainFileURL: mainFileURL),
            kind: kind,
            scope: scope,
            mainFileURL: mainFileURL,
            containerURL: containerURL,
            name: name,
            description: desc,
            rawContent: content,
            fileModifiedAt: mtime,
            sizeBytes: size,
            hasSubdirectories: hasSubs
        )
    }

    private func detectSubdirectories(in container: URL) -> Bool {
        let fm = FileManager.default
        let entries = (try? fm.contentsOfDirectory(at: container,
                                                   includingPropertiesForKeys: [.isDirectoryKey],
                                                   options: [.skipsHiddenFiles])) ?? []
        for entry in entries {
            var isDir: ObjCBool = false
            if fm.fileExists(atPath: entry.path, isDirectory: &isDir), isDir.boolValue {
                return true
            }
        }
        return false
    }
}
```

- [ ] **Step 5: 跑测试确认通过**

⌘U。预期：所有测试 PASS。

如果失败：检查 Fixtures 目录是否被加到 Test target Bundle Resources（Build Phases → Copy Bundle Resources）。

- [ ] **Step 6: Commit**

```bash
git add AISkillManager/Repositories/DirectorySkillRepoBase.swift \
        AISkillManagerTests/DirectorySkillRepoBaseTests.swift \
        AISkillManagerTests/Fixtures/
git commit -m "feat(repo): implement DirectorySkillRepoBase + fixtures + tests"
```

---

## Task 6: ClaudeSkillsRepo / CodexSkillsRepo / CursorSkillsRepo

**Files:**
- Create: `AISkillManager/Repositories/ClaudeSkillsRepo.swift`
- Create: `AISkillManager/Repositories/CodexSkillsRepo.swift`
- Create: `AISkillManager/Repositories/CursorSkillsRepo.swift`

**说明：** 三个具体子类只需各自指定根目录路径。所有逻辑在 base class 里已实现完毕，无需测试（base 已被 Task 5 覆盖）。

- [ ] **Step 1: 实现 ClaudeSkillsRepo.swift**

```swift
import Foundation

final class ClaudeSkillsRepo: DirectorySkillRepoBase {
    static let userRoot: URL = FileManager.default.homeDirectoryForCurrentUser
        .appendingPathComponent(".claude/skills")

    init(scope: SourceScope = .user, root: URL? = nil) {
        super.init(kind: .claudeSkills, scope: scope, root: root ?? Self.userRoot)
    }
}
```

- [ ] **Step 2: 实现 CodexSkillsRepo.swift**

```swift
import Foundation

final class CodexSkillsRepo: DirectorySkillRepoBase {
    static let userRoot: URL = FileManager.default.homeDirectoryForCurrentUser
        .appendingPathComponent(".codex/skills")

    init(scope: SourceScope = .user, root: URL? = nil) {
        super.init(kind: .codexSkills, scope: scope, root: root ?? Self.userRoot)
    }
}
```

- [ ] **Step 3: 实现 CursorSkillsRepo.swift**

```swift
import Foundation

final class CursorSkillsRepo: DirectorySkillRepoBase {
    static let userRoot: URL = FileManager.default.homeDirectoryForCurrentUser
        .appendingPathComponent(".cursor/skills")

    init(scope: SourceScope = .user, root: URL? = nil) {
        super.init(kind: .cursorSkills, scope: scope, root: root ?? Self.userRoot)
    }
}
```

- [ ] **Step 4: 验证 Build + 复用 Task 5 的测试**

⌘B → 编译通过。
⌘U → Task 5 的 base 测试仍 PASS。

- [ ] **Step 5: Commit**

```bash
git add AISkillManager/Repositories/ClaudeSkillsRepo.swift \
        AISkillManager/Repositories/CodexSkillsRepo.swift \
        AISkillManager/Repositories/CursorSkillsRepo.swift
git commit -m "feat(repo): add Claude/Codex/Cursor skills repos"
```

---

## Task 7: CursorRulesRepo（单文件式）

**Files:**
- Create: `AISkillManager/Repositories/CursorRulesRepo.swift`
- Test: `AISkillManagerTests/CursorRulesRepoTests.swift`
- Test fixture: `AISkillManagerTests/Fixtures/cursor-rules/`

**说明：** Cursor Rules 是单 `.mdc` 文件，需要单独实现。

- [ ] **Step 1: 创建 fixture**

`AISkillManagerTests/Fixtures/cursor-rules/global-ai-behavior.mdc`:
```markdown
---
alwaysApply: true
description: Global AI behavior rules
---

# Global rules

Always do X.
```

`AISkillManagerTests/Fixtures/cursor-rules/no-frontmatter.mdc`:
```markdown
This file has no frontmatter at all.
```

`AISkillManagerTests/Fixtures/cursor-rules/empty.mdc`:
```markdown
```

把目录加到 Xcode AISkillManagerTests target。

- [ ] **Step 2: 写失败的测试**

`AISkillManagerTests/CursorRulesRepoTests.swift`:

```swift
import XCTest
@testable import AISkillManager

final class CursorRulesRepoTests: XCTestCase {
    private var fixtureRoot: URL {
        Bundle(for: type(of: self))
            .url(forResource: "cursor-rules", withExtension: nil)!
    }

    private func makeRepo() -> CursorRulesRepo {
        CursorRulesRepo(scope: .user, root: fixtureRoot)
    }

    func test_listAll_finds_mdc_files() throws {
        let items = try makeRepo().listAll()
        let names = Set(items.map(\.name))
        XCTAssertTrue(names.contains("Global AI behavior rules") || names.contains("global-ai-behavior"))
        XCTAssertTrue(names.contains("no-frontmatter"))
    }

    func test_listAll_uses_filename_when_frontmatter_missing() throws {
        let items = try makeRepo().listAll()
        let item = items.first { $0.mainFileURL.lastPathComponent == "no-frontmatter.mdc" }!
        XCTAssertEqual(item.name, "no-frontmatter")
        XCTAssertEqual(item.description, "")
    }

    func test_listAll_uses_description_from_frontmatter() throws {
        let items = try makeRepo().listAll()
        let item = items.first { $0.mainFileURL.lastPathComponent == "global-ai-behavior.mdc" }!
        XCTAssertEqual(item.description, "Global AI behavior rules")
    }

    func test_listAll_containerURL_is_nil() throws {
        let items = try makeRepo().listAll()
        XCTAssertTrue(items.allSatisfy { $0.containerURL == nil })
    }

    func test_listAll_hasSubdirectories_is_always_false() throws {
        let items = try makeRepo().listAll()
        XCTAssertTrue(items.allSatisfy { $0.hasSubdirectories == false })
    }

    func test_listAll_skips_non_mdc_files() throws {
        let extraFile = fixtureRoot.appendingPathComponent("readme.txt")
        try? "x".write(to: extraFile, atomically: true, encoding: .utf8)
        defer { try? FileManager.default.removeItem(at: extraFile) }

        let items = try makeRepo().listAll()
        XCTAssertFalse(items.contains { $0.name == "readme" })
    }

    func test_listAll_returns_empty_for_missing_root() throws {
        let bogus = fixtureRoot.appendingPathComponent("does-not-exist")
        let repo = CursorRulesRepo(scope: .user, root: bogus)
        XCTAssertTrue(try repo.listAll().isEmpty)
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

⌘U。预期：所有测试 fail。

- [ ] **Step 4: 实现 CursorRulesRepo.swift**

```swift
import Foundation

final class CursorRulesRepo: SkillRepository {
    let kind: SourceKind = .cursorRules
    let scope: SourceScope
    let root: URL

    static let userRoot: URL = FileManager.default.homeDirectoryForCurrentUser
        .appendingPathComponent(".cursor/rules")

    init(scope: SourceScope = .user, root: URL? = nil) {
        self.scope = scope
        self.root = root ?? Self.userRoot
    }

    var rootExists: Bool {
        var isDir: ObjCBool = false
        let exists = FileManager.default.fileExists(atPath: root.path, isDirectory: &isDir)
        return exists && isDir.boolValue
    }

    func listAll() throws -> [SkillItem] {
        guard rootExists else { return [] }

        let fm = FileManager.default
        let entries = (try? fm.contentsOfDirectory(at: root,
                                                   includingPropertiesForKeys: [.contentModificationDateKey, .fileSizeKey],
                                                   options: [.skipsHiddenFiles])) ?? []

        var items: [SkillItem] = []
        for fileURL in entries {
            guard fileURL.pathExtension == "mdc" else { continue }
            guard let item = makeItem(fileURL: fileURL) else { continue }
            items.append(item)
        }
        return items.sorted { $0.name.localizedCaseInsensitiveCompare($1.name) == .orderedAscending }
    }

    private func makeItem(fileURL: URL) -> SkillItem? {
        guard let content = try? String(contentsOf: fileURL, encoding: .utf8) else { return nil }

        let attrs = (try? FileManager.default.attributesOfItem(atPath: fileURL.path)) ?? [:]
        let mtime = (attrs[.modificationDate] as? Date) ?? Date()
        let size = (attrs[.size] as? Int) ?? 0

        let parsed = YAMLFrontmatter.parseShallow(from: content)
        let baseName = fileURL.deletingPathExtension().lastPathComponent
        let name = parsed.name ?? baseName
        let desc = parsed.description ?? ""

        return SkillItem(
            id: SkillItemID.make(kind: kind, scope: scope, mainFileURL: fileURL),
            kind: kind,
            scope: scope,
            mainFileURL: fileURL,
            containerURL: nil,
            name: name,
            description: desc,
            rawContent: content,
            fileModifiedAt: mtime,
            sizeBytes: size,
            hasSubdirectories: false
        )
    }
}
```

- [ ] **Step 5: 跑测试确认通过**

⌘U。预期：所有 PASS。

- [ ] **Step 6: Commit**

```bash
git add AISkillManager/Repositories/CursorRulesRepo.swift \
        AISkillManagerTests/CursorRulesRepoTests.swift \
        AISkillManagerTests/Fixtures/cursor-rules/
git commit -m "feat(repo): implement CursorRulesRepo for .mdc single-file source"
```

---

## Task 8: AppStore（@Observable 单一状态源）

**Files:**
- Create: `AISkillManager/ViewModels/AppStore.swift`
- Test: `AISkillManagerTests/AppStoreTests.swift`

**职责：** 持有 4 个 Repo、加载所有 SkillItem、维护"当前选中源 / 当前选中条目"状态。M1 还不做编辑缓冲。

- [ ] **Step 1: 写失败的测试**

`AISkillManagerTests/AppStoreTests.swift`:

```swift
import XCTest
@testable import AISkillManager

final class AppStoreTests: XCTestCase {
    /// 提供一组 in-memory 的 Repo（指向 Fixtures），便于不依赖真实 ~/ 路径
    private func makeStubStore() -> AppStore {
        let dirRoot = Bundle(for: type(of: self))
            .url(forResource: "dir-source", withExtension: nil)!
        let mdcRoot = Bundle(for: type(of: self))
            .url(forResource: "cursor-rules", withExtension: nil)!

        let repos: [SkillRepository] = [
            DirectorySkillRepoBase(kind: .claudeSkills, scope: .user, root: dirRoot),
            DirectorySkillRepoBase(kind: .codexSkills,  scope: .user, root: dirRoot),
            DirectorySkillRepoBase(kind: .cursorSkills, scope: .user, root: dirRoot),
            CursorRulesRepo(scope: .user, root: mdcRoot),
        ]
        return AppStore(repos: repos)
    }

    func test_initial_state_is_empty() {
        let store = makeStubStore()
        XCTAssertTrue(store.itemsBySource.isEmpty)
        XCTAssertNil(store.selectedSourceKey)
        XCTAssertNil(store.selectedItemID)
    }

    func test_loadAll_populates_items_grouped_by_source() async {
        let store = makeStubStore()
        await store.loadAll()
        XCTAssertEqual(store.itemsBySource.count, 4)

        let claudeKey = AppStore.sourceKey(kind: .claudeSkills, scope: .user)
        XCTAssertEqual(store.itemsBySource[claudeKey]?.count, 4) // dir-source 里 4 条
    }

    func test_loadAll_auto_selects_first_nonempty_source() async {
        let store = makeStubStore()
        await store.loadAll()
        XCTAssertNotNil(store.selectedSourceKey)
        XCTAssertNotNil(store.selectedItemID)
    }

    func test_select_source_changes_selectedSourceKey() async {
        let store = makeStubStore()
        await store.loadAll()
        let codexKey = AppStore.sourceKey(kind: .codexSkills, scope: .user)
        store.selectSource(codexKey)
        XCTAssertEqual(store.selectedSourceKey, codexKey)
    }

    func test_select_source_auto_selects_first_item_in_that_source() async {
        let store = makeStubStore()
        await store.loadAll()
        let codexKey = AppStore.sourceKey(kind: .codexSkills, scope: .user)
        store.selectSource(codexKey)
        XCTAssertNotNil(store.selectedItemID)
        XCTAssertEqual(store.currentItem?.kind, .codexSkills)
    }

    func test_currentItem_returns_selected() async {
        let store = makeStubStore()
        await store.loadAll()
        let claudeKey = AppStore.sourceKey(kind: .claudeSkills, scope: .user)
        store.selectSource(claudeKey)
        let firstItem = store.itemsBySource[claudeKey]!.first!
        store.selectedItemID = firstItem.id
        XCTAssertEqual(store.currentItem?.id, firstItem.id)
    }

    func test_currentItem_nil_when_id_not_found() async {
        let store = makeStubStore()
        await store.loadAll()
        store.selectedItemID = SkillItemID(kind: .claudeSkills, scopeKey: "user", pathFingerprint: "deadbeef0000")
        XCTAssertNil(store.currentItem)
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

⌘U。预期：失败（AppStore 未定义）。

- [ ] **Step 3: 实现 AppStore.swift**

```swift
import Foundation
import Observation

@Observable
final class AppStore {
    /// 唯一区分一个数据源的 key：kind + scope.key
    typealias SourceKey = String
    static func sourceKey(kind: SourceKind, scope: SourceScope) -> SourceKey {
        "\(kind.rawValue):\(scope.key)"
    }

    private let repos: [SkillRepository]

    /// source key → 该源下所有 items
    var itemsBySource: [SourceKey: [SkillItem]] = [:]

    /// 当前选中源 key
    var selectedSourceKey: SourceKey?

    /// 当前选中条目 ID
    var selectedItemID: SkillItemID?

    /// 加载状态
    var isLoading = false

    /// 加载错误（按源 key 分别记录）
    var loadErrors: [SourceKey: String] = [:]

    init(repos: [SkillRepository]) {
        self.repos = repos
    }

    /// 默认构造：4 个用户级 Repo
    static func makeDefault() -> AppStore {
        AppStore(repos: [
            ClaudeSkillsRepo(),
            CodexSkillsRepo(),
            CursorSkillsRepo(),
            CursorRulesRepo(),
        ])
    }

    /// 当前选中条目（计算属性）
    var currentItem: SkillItem? {
        guard let id = selectedItemID,
              let key = selectedSourceKey,
              let items = itemsBySource[key] else { return nil }
        return items.first { $0.id == id }
    }

    /// 全量加载所有源
    @MainActor
    func loadAll() async {
        isLoading = true
        defer { isLoading = false }

        var grouped: [SourceKey: [SkillItem]] = [:]
        var errors: [SourceKey: String] = [:]

        for repo in repos {
            let key = Self.sourceKey(kind: repo.kind, scope: repo.scope)
            do {
                let items = try repo.listAll()
                grouped[key] = items
            } catch {
                grouped[key] = []
                errors[key] = String(describing: error)
            }
        }

        itemsBySource = grouped
        loadErrors = errors

        // 自动选中第一个非空源 + 该源第一条
        if selectedSourceKey == nil {
            if let firstKey = repos
                .map({ Self.sourceKey(kind: $0.kind, scope: $0.scope) })
                .first(where: { (grouped[$0]?.isEmpty == false) })
            {
                selectSource(firstKey)
            }
        }
    }

    /// 切换选中源（自动选中该源第一条）
    func selectSource(_ key: SourceKey) {
        selectedSourceKey = key
        selectedItemID = itemsBySource[key]?.first?.id
    }

    /// 列出所有 source key（按 repos 顺序）
    var allSourceKeys: [SourceKey] {
        repos.map { Self.sourceKey(kind: $0.kind, scope: $0.scope) }
    }

    /// 该 source key 对应的 kind / scope（用于 UI 显示）
    func sourceMeta(for key: SourceKey) -> (kind: SourceKind, scope: SourceScope)? {
        guard let repo = repos.first(where: { Self.sourceKey(kind: $0.kind, scope: $0.scope) == key }) else {
            return nil
        }
        return (repo.kind, repo.scope)
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

⌘U。预期：所有 PASS。

- [ ] **Step 5: Commit**

```bash
git add AISkillManager/ViewModels/AppStore.swift AISkillManagerTests/AppStoreTests.swift
git commit -m "feat(vm): add AppStore @Observable with multi-source load"
```

---

## Task 9: SidebarView（数据源列表）

**Files:**
- Create: `AISkillManager/Views/SidebarView.swift`

**说明：** Views 层我们采用"最小化 + 手测"的策略——M1 不写 snapshot 测试（Plan 4 打磨阶段加）。

- [ ] **Step 1: 实现 SidebarView**

```swift
import SwiftUI

struct SidebarView: View {
    @Bindable var store: AppStore

    var body: some View {
        List(selection: Binding(
            get: { store.selectedSourceKey },
            set: { newValue in
                if let key = newValue {
                    store.selectSource(key)
                }
            }
        )) {
            Section("用户级") {
                ForEach(store.allSourceKeys, id: \.self) { key in
                    if let meta = store.sourceMeta(for: key) {
                        sidebarRow(key: key, meta: meta)
                            .tag(Optional(key))
                    }
                }
            }
        }
        .listStyle(.sidebar)
        .frame(minWidth: 200)
    }

    @ViewBuilder
    private func sidebarRow(key: AppStore.SourceKey, meta: (kind: SourceKind, scope: SourceScope)) -> some View {
        HStack {
            Image(systemName: meta.kind.iconSymbol)
                .frame(width: 18)
            Text(meta.kind.displayName)
            Spacer()
            Text("\(store.itemsBySource[key]?.count ?? 0)")
                .font(.caption)
                .foregroundStyle(.secondary)
                .padding(.horizontal, 6)
                .background(Color.secondary.opacity(0.15))
                .clipShape(Capsule())
        }
    }
}
```

- [ ] **Step 2: 验证 Build**

⌘B。预期：通过。

- [ ] **Step 3: Commit**

```bash
git add AISkillManager/Views/SidebarView.swift
git commit -m "feat(view): add SidebarView with source list and counts"
```

---

## Task 10: ItemListView（中间条目列表）

**Files:**
- Create: `AISkillManager/Views/ItemListView.swift`

- [ ] **Step 1: 实现 ItemListView**

```swift
import SwiftUI

struct ItemListView: View {
    @Bindable var store: AppStore

    private var items: [SkillItem] {
        guard let key = store.selectedSourceKey else { return [] }
        return store.itemsBySource[key] ?? []
    }

    var body: some View {
        List(selection: $store.selectedItemID) {
            ForEach(items) { item in
                row(for: item)
                    .tag(Optional(item.id))
            }
        }
        .listStyle(.inset(alternatesRowBackgrounds: false))
        .frame(minWidth: 280)
        .overlay {
            if items.isEmpty {
                ContentUnavailableView("没有条目", systemImage: "tray", description: Text("此源下还没有 skill 文件"))
            }
        }
    }

    @ViewBuilder
    private func row(for item: SkillItem) -> some View {
        VStack(alignment: .leading, spacing: 3) {
            Text(item.name)
                .font(.system(size: 13, weight: .semibold))
                .lineLimit(1)
            if !item.description.isEmpty {
                Text(item.description)
                    .font(.system(size: 11.5))
                    .foregroundStyle(.secondary)
                    .lineLimit(2)
            }
            HStack(spacing: 6) {
                Text(item.scope.displayLabel)
                    .font(.system(size: 10))
                    .padding(.horizontal, 5).padding(.vertical, 1)
                    .background(Color.accentColor.opacity(0.15))
                    .foregroundStyle(Color.accentColor)
                    .clipShape(RoundedRectangle(cornerRadius: 3))
                Text(item.fileModifiedAt, style: .date)
                    .font(.system(size: 10))
                    .foregroundStyle(.tertiary)
                if item.hasSubdirectories {
                    Image(systemName: "folder")
                        .font(.system(size: 9))
                        .foregroundStyle(.tertiary)
                }
            }
        }
        .padding(.vertical, 2)
    }
}
```

- [ ] **Step 2: 验证 Build**

⌘B。预期：通过。

- [ ] **Step 3: Commit**

```bash
git add AISkillManager/Views/ItemListView.swift
git commit -m "feat(view): add ItemListView with name/description/badge rows"
```

---

## Task 11: DetailView（M1 只读展示）

**Files:**
- Create: `AISkillManager/Views/DetailView.swift`

**说明：** M1 用 `TextEditor` 只读展示 rawContent（暂不接入语法高亮，那是 M2 的事）。

- [ ] **Step 1: 实现 DetailView**

```swift
import SwiftUI

struct DetailView: View {
    @Bindable var store: AppStore

    var body: some View {
        if let item = store.currentItem {
            VStack(spacing: 0) {
                toolbar(for: item)
                Divider()
                ScrollView {
                    Text(item.rawContent)
                        .font(.system(size: 13, design: .monospaced))
                        .frame(maxWidth: .infinity, alignment: .leading)
                        .padding(16)
                        .textSelection(.enabled)
                }
                .background(Color(nsColor: .textBackgroundColor))
                statusBar(for: item)
            }
        } else {
            ContentUnavailableView("未选中条目",
                                   systemImage: "doc.text",
                                   description: Text("从左侧选择一个 skill 查看内容"))
        }
    }

    @ViewBuilder
    private func toolbar(for item: SkillItem) -> some View {
        HStack(spacing: 8) {
            Text(item.mainFileURL.path(percentEncoded: false))
                .font(.system(size: 11, design: .monospaced))
                .foregroundStyle(.secondary)
                .lineLimit(1)
                .truncationMode(.middle)
            Spacer()
            Button {
                NSWorkspace.shared.activateFileViewerSelecting([item.mainFileURL])
            } label: {
                Label("在 Finder 中显示", systemImage: "folder")
            }
            Button {
                let pasteboard = NSPasteboard.general
                pasteboard.clearContents()
                pasteboard.setString(item.mainFileURL.path(percentEncoded: false), forType: .string)
            } label: {
                Label("复制路径", systemImage: "doc.on.doc")
            }
        }
        .padding(.horizontal, 12).padding(.vertical, 8)
        .background(Color(nsColor: .windowBackgroundColor))
    }

    @ViewBuilder
    private func statusBar(for item: SkillItem) -> some View {
        HStack(spacing: 18) {
            Text("UTF-8")
            Text(item.kind == .cursorRules ? "Markdown (Cursor MDC)" : "YAML + Markdown")
            Text("\(item.sizeBytes) bytes")
            Spacer()
            Text("只读 (M1)")
        }
        .font(.system(size: 10, design: .monospaced))
        .padding(.horizontal, 12).padding(.vertical, 4)
        .background(Color.accentColor)
        .foregroundStyle(.white)
    }
}
```

- [ ] **Step 2: 验证 Build**

⌘B。预期：通过。

- [ ] **Step 3: Commit**

```bash
git add AISkillManager/Views/DetailView.swift
git commit -m "feat(view): add read-only DetailView with toolbar and status bar"
```

---

## Task 12: AppRoot 三栏布局 + 启动加载

**Files:**
- Create: `AISkillManager/App/AppRoot.swift`
- Modify: `AISkillManager/AISkillManagerApp.swift`

- [ ] **Step 1: 实现 AppRoot**

```swift
import SwiftUI

struct AppRoot: View {
    @State private var store = AppStore.makeDefault()

    var body: some View {
        NavigationSplitView {
            SidebarView(store: store)
        } content: {
            ItemListView(store: store)
        } detail: {
            DetailView(store: store)
        }
        .frame(minWidth: 1000, minHeight: 600)
        .task {
            await store.loadAll()
        }
        .toolbar {
            ToolbarItem(placement: .primaryAction) {
                Button {
                    Task { await store.loadAll() }
                } label: {
                    Label("刷新", systemImage: "arrow.clockwise")
                }
            }
        }
    }
}
```

- [ ] **Step 2: 修改 AISkillManagerApp.swift**

```swift
import SwiftUI

@main
struct AISkillManagerApp: App {
    var body: some Scene {
        WindowGroup {
            AppRoot()
        }
        .windowResizability(.contentMinSize)
    }
}
```

- [ ] **Step 3: 验证 Build**

⌘B。预期：通过。

- [ ] **Step 4: 跑应用做手测**

⌘R 启动 App。预期：

- 看到三栏窗口
- 左侧 Sidebar 显示 4 个数据源（Claude Skills / Codex Skills / Cursor Skills / Cursor Rules），右侧带数字 badge
- 中间 List 自动选中第一个非空源、显示该源所有 skill 条目
- 右侧 Detail 自动选中第一条、显示 SKILL.md 原文
- 点击 Sidebar 切换源、点击 List 切换条目，Detail 同步更新
- 点击 toolbar 刷新按钮，重新扫描

**手测检查清单：**

- [ ] Claude Skills 列表里能看到你 `~/.claude/skills/` 下的 design / web-design-tw93 / takeover-context 等条目
- [ ] Codex Skills 列表里能看到 playwright / defuddle / json-canvas 等
- [ ] Cursor Rules 列表里能看到 global-ai-behavior
- [ ] 选中某条 → 右侧 Detail 显示完整 SKILL.md / .mdc 原文
- [ ] 工具栏的"在 Finder 中显示"按钮能正确弹出 Finder 并定位到文件
- [ ] 工具栏的"复制路径"按钮能复制路径到剪贴板（粘贴到终端验证）
- [ ] ⌘W 关窗、⌘Q 退出、⌘M 最小化按 macOS 习惯工作

如发现问题：先 git stash 保留改动，回到上一个绿色 commit 看 diff。

- [ ] **Step 5: Commit**

```bash
git add AISkillManager/App/AppRoot.swift AISkillManager/AISkillManagerApp.swift
git commit -m "feat(app): wire three-pane AppRoot with auto-load on launch"
```

---

## Task 13: 处理符号链接的 skill 条目

**Files:**
- Modify: `AISkillManager/Repositories/DirectorySkillRepoBase.swift`
- Test: 在 `AISkillManagerTests/DirectorySkillRepoBaseTests.swift` 追加

**背景：** 你机器上 `~/.claude/skills/find-skills` 是个 symlink，`~/.codex/skills/clawd-cursor` 也是。当前 base 已经能扫到（FileManager 默认跟随 symlink），但要加测试确认。

- [ ] **Step 1: 创建 symlink fixture**

在 Xcode 中无法直接 commit symlink。我们用程序在 Test setUp 时建立：

在 `DirectorySkillRepoBaseTests.swift` 头部加：

```swift
private func makeSymlinkFixture(in root: URL) throws -> URL {
    // 已有 skill-good 目录，建一个 symlink 指向它
    let linkURL = root.appendingPathComponent("skill-symlinked")
    let targetURL = root.appendingPathComponent("skill-good")

    // 清理上次残留
    try? FileManager.default.removeItem(at: linkURL)

    try FileManager.default.createSymbolicLink(at: linkURL, withDestinationURL: targetURL)
    return linkURL
}
```

- [ ] **Step 2: 写失败的测试**

在 `DirectorySkillRepoBaseTests` 类内追加：

```swift
func test_listAll_includes_symlinked_directories() throws {
    let link = try makeSymlinkFixture(in: fixtureRoot)
    defer { try? FileManager.default.removeItem(at: link) }

    let items = try makeRepo().listAll()
    XCTAssertTrue(items.contains { $0.name == "skill-symlinked" || $0.name == "skill-good" })

    // 找到 symlinked 那条（注意 name 可能是 frontmatter 的 "skill-good"）
    let symlinked = items.first { $0.mainFileURL.path.contains("skill-symlinked") }
    XCTAssertNotNil(symlinked)
}
```

- [ ] **Step 3: 跑测试确认通过（base 已支持，可能直接 PASS）**

⌘U。预期：PASS（FileManager 默认跟随 symlink）。

如果失败：在 `listAll()` 里检查文件存在判断时，把 `fileExists(atPath:isDirectory:)` 改用 resolved path：

```swift
let resolvedPath = (try? FileManager.default.destinationOfSymbolicLink(atPath: dir.path)) ?? dir.path
guard fm.fileExists(atPath: resolvedPath, isDirectory: &isDir), isDir.boolValue else { continue }
```

- [ ] **Step 4: Commit**

```bash
git add AISkillManagerTests/DirectorySkillRepoBaseTests.swift
git commit -m "test: add symlinked-directory case for DirectorySkillRepoBase"
```

---

## Task 14: 加载错误显示（基础）

**Files:**
- Modify: `AISkillManager/Views/SidebarView.swift`

**背景：** AppStore 已经记录了 `loadErrors`，但 Sidebar 还没显示。无错误时正常，有错误时给条目右侧加个红色 ⚠️。

- [ ] **Step 1: 修改 SidebarView**

把 `sidebarRow` 函数改成：

```swift
@ViewBuilder
private func sidebarRow(key: AppStore.SourceKey, meta: (kind: SourceKind, scope: SourceScope)) -> some View {
    HStack {
        Image(systemName: meta.kind.iconSymbol)
            .frame(width: 18)
        Text(meta.kind.displayName)
        Spacer()
        if store.loadErrors[key] != nil {
            Image(systemName: "exclamationmark.triangle.fill")
                .foregroundStyle(.orange)
                .help(store.loadErrors[key] ?? "")
        }
        Text("\(store.itemsBySource[key]?.count ?? 0)")
            .font(.caption)
            .foregroundStyle(.secondary)
            .padding(.horizontal, 6)
            .background(Color.secondary.opacity(0.15))
            .clipShape(Capsule())
    }
}
```

- [ ] **Step 2: 验证 Build + 手测**

⌘R 启动。预期：用户级 4 个源都正常加载，无 ⚠️。

- [ ] **Step 3: Commit**

```bash
git add AISkillManager/Views/SidebarView.swift
git commit -m "feat(view): show error badge in sidebar when source load fails"
```

---

## Task 15: M1 完结自查与文档

**Files:**
- Modify: `docs/superpowers/specs/2026-04-28-ai-skill-manager-design.md`（在 §11 末尾标记 M1 完成）

- [ ] **Step 1: 全量回归**

⌘U 跑所有测试。预期：全 PASS（YAMLFrontmatter / DirectorySkillRepoBase / CursorRulesRepo / AppStore 全部）。

- [ ] **Step 2: 手测验收清单**

⌘R 启动 App，确认：

- [ ] 4 个源全部显示，每个 badge 数字正确（对照 Finder 数）
- [ ] 选中切换流畅，无卡顿
- [ ] 路径面包屑正确
- [ ] 在 Finder 中显示 / 复制路径都工作
- [ ] 点刷新按钮重新扫描，新增/删除文件能反映出来
  - 测试方法：在终端 `touch ~/.claude/skills/test-skill/SKILL.md` 后回 App 点刷新
- [ ] 退出再启动，App 能正常打开（无 crash）

- [ ] **Step 3: 在 spec 末尾标记 M1 状态**

打开 `docs/superpowers/specs/2026-04-28-ai-skill-manager-design.md`，在 §11 实施里程碑表格后追加：

```markdown
**M1 进度**: ✅ 完成于 YYYY-MM-DD。Plan 文件：`docs/superpowers/plans/2026-04-28-m1-baseline.md`
```

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/specs/2026-04-28-ai-skill-manager-design.md
git commit -m "docs: mark M1 milestone complete"
```

- [ ] **Step 5: 创建 git tag**

```bash
git tag -a m1-baseline -m "M1: read-only browsing of 4 user-level sources"
git log --oneline | head -20
```

---

## 完结状态

跑完 Task 1-15 后：

- App 是一个能日常使用的"只读 skill 浏览器"
- 4 个用户级源全部支持
- 三栏布局 + 工具栏 + 状态栏
- 所有 Repository 和 ViewModel 单元测试覆盖
- 已打 tag `m1-baseline`

**下一步：** 写 `docs/superpowers/plans/<date>-m2-editing.md` 加入 Raw 编辑器、保存流程、聚焦刷新。

---

## Self-Review 备忘

- ✅ Spec §3（数据源） / §5（数据模型） / §6（组件拆分） / §7.1（启动）完整覆盖
- ✅ Spec §8 错误处理：dotfile 过滤、symlink、路径不存在、frontmatter 解析失败、无 description 全部测试覆盖
- ✅ Spec §10 测试策略：Repository 单测 + AppStore 集成测试，View 暂用手测（M4 加 snapshot）
- ⏸️ Spec §7.2-7.6 编辑/新建/删除/聚焦刷新/搜索 → M2 / Plan 2 实现
- ⏸️ Spec §9 分发 → Plan 3 实现
